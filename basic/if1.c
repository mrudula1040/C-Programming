#include<stdio.h>
int main(){
    int num;
    printf("enter number:");
    scanf("%d",&num);
    if(num>=0){
        printf("number =%d is positive number",num);

    }else{
        printf("number = %d is neggetive num",num);
    }
}